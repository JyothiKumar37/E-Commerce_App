# ArgoCD Application: kube-prometheus-stack
# Multi-source: the upstream Helm chart plus this repo's values file ($values).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-prometheus-stack
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: monitoring
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  sources:
    - repoURL: https://prometheus-community.github.io/helm-charts
      chart: kube-prometheus-stack
      targetRevision: "88.5.4"
      helm:
        valueFiles:
          - $values/monitoring/values/kube-prometheus-stack.yaml
    - repoURL: https://github.com/JyothiKumar37/E-Commerce_App.git
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
